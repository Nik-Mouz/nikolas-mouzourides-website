version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - yarn install
  build:
    commands:
      - yarn build
      - yarn test:ci
      - aws s3 sync --delete --no-progress public/ s3://nikmouz.dev
artifacts:
  files:
    - reports/**/*
  name: reports_$(date +%Y-%m-%d_%H:%M:%S)
cache:
  paths:
    - node_modules/**/*
